checksumBehavior: reset

nodeLinker: node-modules

# GHSA-qwww-vcr4-c8h2 affects only React Router's unstable RSC APIs. This
# project is a client-only Vite SPA and does not import or enable RSC mode.
npmAuditIgnoreAdvisories:
  - "1124282"
